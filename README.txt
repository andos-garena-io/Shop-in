ANDOS REPO UPLOAD GUIDE
=======================
Is zip ke sab files ko GitHub repo ke ROOT me daalo:

1) index.html      -> root (main app)
2) support.html    -> root (ANDOS Support AI chat - orders page ke 🎧 button se khulta hai, back arrow se index pe wapas)
3) manifest.json   -> root (optional, PWA install)
4) sw.js           -> root (optional, offline cache)

Images (ff-card, combo-card, featured-plans, membership-plans, mega-packs,
monthly-membership-offer, ff-guild) already dusre repo ke
assets/FF-PLAN-520/ folder me hain (CDN se load hoti hain) - kuch nahi karna.

Bot commands: /stats /refs /payment <username> /help
